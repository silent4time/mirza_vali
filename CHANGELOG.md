# mirza_vali — Changelog

## v1.1.0 (2026-08-02)

### منوی مدیریت
- اسکریپت `manage.sh` با منوی کامل:
  - **1 نصب** — نصب کامل از صفر
  - **2 آپدیت** — به‌روزرسانی پچ بدون از دست دادن دیتابیس و config
  - **3 ریست** — وبهوک / تونل / step کاربران
  - **4 حذف کامل** — فایل‌ها + دیتابیس + nginx + سرویس
  - **5 وضعیت** — نسخه، مسیر، سرویس‌ها
  - **0 خروج**
- پشتیبانی از آرگومان خط فرمان: `install | update | reset | remove | status`
- ذخیره وضعیت نصب در `/etc/mirza_vali/install.env`
- آماده برای میزبانی روی GitHub و نصب یک‌خطی

### ساختار ریپو
- پوشه `patch/` حاوی فایل‌های اصلاح‌شده
- نصب از کلون ریپو (بدون embed حجیم base64)

---

## v1.0.0 (2026-08-02)

### نام پروژه
- پروژه به نام **mirza_vali** تغییر یافت.
- مسیر نصب پیش‌فرض: `/var/www/mirza_vali`
- نام دیتابیس پیش‌فرض: `mirza_vali`

### رفع باگ‌های پرداخت

1. **بله‌پی — پیام «این روش پرداخت هنوز توسط مدیر تنظیم نشده است»**
   - توکن پرداخت (`merchant_balepay`) در لحظه استفاده از config و PaySetting خوانده می‌شود.
   - اگر توکن در زمان نصب وارد شود، در دیتابیس ذخیره و وضعیت روشن می‌شود.

2. **کارت‌به‌کارت در بله دیگر به تلگرام هدایت نمی‌کند**
   - برای کاربران بله همیشه از `callback_data: cart_to_offline` استفاده می‌شود.

3. **جداسازی پلتفرم برای پرداخت**
   - دکمه بله‌پی برای هر دو پلتفرم.
   - کاربر بله: فاکتور مستقیم.
   - کاربر تلگرام + بله‌پی: کد سفارش + راهنما + گزارش به ادمین.
